OMNIPATH ENGINE - TECHNICAL DOCUMENTATION AND ARCHITECTURE REFERENCE (VERSION 5.1)

This document provides deep technical insight into the OmniPath Engine 5.1, 
the quantitative core of the ETF Portfolio Simulator. It addresses the 
architectural conventions, stochastic models, and performance optimizations 
that enable complex financial mathematics to be executed in real-time within the browser.


CHAPTER 1: QUANTITATIVE MODELS & FINANCIAL ENGINEERING

The OmniPath Engine abstracts fundamental market dynamics into a stochastic 
differential equation (SDE) system. It calculates not only normal distributions 
but also empirically substantiated asymmetric crisis scenarios.

- HAWKES-PROCESS (CONTAGION CLUSTERING)
  Instead of rigid, homogeneous Poisson distributions, the engine implements a 
  time-discrete, self-exciting Hawkes process. Extreme market crashes fluctuate in logical sequential shocks. 
  This model simulates authentic empirical contagion clusters and correctly dynamizes 
  extreme asymmetric -70% drawdowns mathematically via Exact Exponential CDF boundaries, allowing systemic crises 
  to realistically persist over prolonged durations.

- 5-FACTOR REGIONAL STRUCTURE (DOM-BINDING)
  The internal JSON payload governing the portfolios (`data.js`) rigidly enforces a 5-dimensional 
  geographical isolation lattice (`North America`, `Europe`, `Emerging Markets`, `Japan`, `Pacific`). 
  This explicit array coupling prevents `undefined` geometry mapping errors during Chart.js rendering 
  and successfully supersedes heavily flawed legacy 3-factor models.

- HESTON STOCHASTIC VOLATILITY
  Variances fluctuate fully stochastically based on a calibrated Cox-Ingersoll-Ross (CIR) process. 
  To natively map empirical market drawdowns, the diffusion integrates a strict bivariate leverage correlation 
  parameter (rho = -0.7) mathematically coupling explosive upside variance permutations exclusively to severe portfolio crashes.
  To prevent systemic critical negative variances native to the Euler-Maruyama 
  approximation procedure (which would lead to fatal NaN collapses), the OmniPath Engine 
  rigorously applies the computationally stable Lord/Koekkoek "Full Truncation" scheme. 
  Furthermore, a strict 80% jump-variance cap securely shields the geometric Feller condition 
  from underlying diffusion collapses.

- LOGNORMAL MARTINGALE COMPENSATOR
  Because asymmetric crash spikes aggressively drive distribution logics south, the system integrates a 
  stationary drift compensator. This ensures the median-targeted P50 expected-value level 
  of the simulation is offset geometrically, balancing structural distortions and safely preserving 
  the geometric portfolio paths against unjust backward skewing.

- LINEAR VOLATILITY SCALING (MERTON JUMPS)
  To prevent extreme dimensional anomalies, systemic shocks are formatted precisely 
  utilizing strict linear volatility scaling mapping. This mathematically maintains the dispersion 
  and correlation laws intact across the different ETF risk classes, guaranteeing low-risk 
  bond allocations scale logically against fat-tail jump events, eliminating runaway severity ratios.

- DYNAMIC CONDITIONAL CORRELATION
  During turbulent market regimes, asset correlations do not jump blindly toward a binary "1.0", 
  but interpolate continuously. The dynamic sliding intensity parameter integrates natively 
  with the Cholesky-decomposition matrix, flawlessly bridging physical diversification 
  divergences generating highly realistic structural stress deviations. This natively includes 
  Spherical Normalizations to rigorously maintain exact unit-variance boundaries.

- HULL-WHITE INTEREST RATE DYNAMICS
  Inflation and baseline real rates are heavily discretized inherently utilizing a continuous 
  SDE mean-reverting infrastructure. Replacing flawed limit-bound Euler-Maruyama drifts, 
  the engine natively executes the Exact Analytical Ornstein-Uhlenbeck Transition arrays, 
  locking exact stationary bounds irrespective of macroscopic non-continuous step limits (dt = 1/12).

- FISHER-DISCOUNTED REAL-PV
  Cumulative portfolio capitalizations and benchmark cashflow distributions natively discard 
  deterministic bifurcated standard deflators. The engine dynamically maps homomorphic path-dependent 
  equivalents tracking exact probabilistic expectations strictly via the integrated Hull-White 
  stochastic bounds, stripping multi-decade valuation decay geometrically cleanly.
- MID-PERIOD SDE CONVENTIONS
  Monthly deposited saving allocations structurally sidestep elementary arithmetic accumulation drags 
  by natively coupling cashflows directly to the active stochastic asset distribution natively. 
  This avoids 'Shannon's Demon' hedging anomalies, pushing investments onto uninterrupted exponential curves.

- LOGARITHMIC TER EXTRACTORS
  Fund management deduction loads naturally depart native geometric arithmetic routines 
  executing mathematical proportionality cleanly inside the exponential SDE matrices as a direct 
  Log-addition ($\ln(1+CAGR) + \ln(1-TER)$). This geometric scalar executes purely drift-neutral.

- O(1) MAXIMUM DRAWDOWN TRACKER
  Ongoing MDD peak-to-trough sequence constants are recursively derived strictly for 
  tens of millions of temporal execution points instantly within an absolute constant bound O(1). 
  Isolating local high-water marks ensures massive history vector matrices avoid consuming memory, 
  saving hundreds of megabytes of browser RAM overhead allocation effortlessly.


CHAPTER 2: ARCHITECTURE & PERFORMANCE OPTIMIZATIONS

The OmniPath Engine serves as a rigid zero-dependency architecture. It accomplishes 
its massive parallel generation speeds in client browsers by executing persistent array pooling techniques 
and absolute hardware-level threading isolations perfectly.

- ASYNCHRONOUS MULTITHREADING
  Isolated Web Worker instances execute highly intensive matrix arrays seamlessly computing 
  100,000 algorithmic trajectories far away from main thread boundaries. This rigidly decouples V8 tracking 
  ensuring flawless 60 FPS zero-latency UI responsiveness globally. Hard spinlocks and preemptive 
  'terminate()' logic unconditionally suppresses parallel underlying zombie-worker state collapses.

- ZERO-COPY IPC (MEMORY TRANSFER) & FLOAT64 POOLING
  Direct ownership transfers of structural ArrayBuffers exclusively execute utilizing Zero-Copy 
  Transferable Objects. This absolutely omits lagging structured-clone cycles eliminating fully native 
  Garbage-Collection (GC) lag spikes through sustainable Capacity-based High-Water-Mark floating limits, 
  effectively bypassing shrinking memory clearance sweeps.

- 128-BIT SFC32 PRNG (HIGH-ENTROPY)
  Constructed securely utilizing ChaCha baseline topologies, this continuous pseudo-random number architecture 
  totally overrides rudimentary browser 'Math.random()' generation. By strictly assigning perfectly decoupled 
  128-bit inner seeds per worker node, tail-risk sequences guarantee maximally uncorrelated sequences accurately.

- O(N) RANDOMIZED QUICKSELECT
  Resolving heavy asymmetrical tail percentiles (P10, P50, P90) natively isolates critical mathematical bounding limits 
  instantly pivoting linearly inside an absolute bounded O(N) constant limit bypassing lagging internal tree sorting logic. 
  "State-Bleed" across random selection trees is securely nullified via a deterministically disjoint 
  `qs_seed` allocation statically offset at Worker Initialization. This definitively restores 
  perfect deterministic Common Random Numbers (CRN) alignment without any O(N^2) degradation traps.
